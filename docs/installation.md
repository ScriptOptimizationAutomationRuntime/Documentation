Downloaded off of www.soardownload.com: 

First unzip the .zip folder/double click it, after that make 
sure you have python installed. You can run it in the terminal or in visual studio code. Run the 
install.py, make sure you have everything thats in the requirements_and_install.txt. 
To run the soar_main.py on macbook you may have to right click the file and open with python if it 
says the developer is not verified. If not just double click it. 

Downloaded off of github: 

Open the .zip file/double click it. Open the folder, then open soar_data. Run the install.py 
and make sure you have everything in requirements_and_install.txt. Then make soar-env. Instructions 
on how to make it are on github.com/ScriptOptimizationAutomationRuntime/latest-version/releases on the 
release you got. To run the soar_main.py on macbook you may have to right click the file and open with
python if it says the developer is not verified. If not just double click it.
